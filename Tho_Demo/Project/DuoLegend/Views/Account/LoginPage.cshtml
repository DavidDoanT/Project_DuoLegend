@model LoginInfor
<link href="~/css/Login.css" rel="stylesheet" />
<script src="https://ajax.aspnetcdn.com/ajax/jQuery/jquery-3.2.1.min.js"></script>
<script src="https://ajax.aspnetcdn.com/ajax/jquery.validate/1.9/jquery.validate.min.js" type="text/javascript"></script>
<style>
    label.error {
        color: red;
    }
</style>
<script>
    $().ready(function () {
        $("#Form").validate({
            rules: {
                "email": {
                    required: true,
                    email: true,
                },

                "password": {
                    required: true,
                    maxlength: 32
                },
            },
            messages: {
                "email": {
                    required: "Email address is required",
                    email: "Email address is invalid"
                },
                "password": {
                    required: "Password is required",
                    maxlength: "Password is too long"
                },
            }
        });
    });
</script>
@*<div class="container">
        <center>
            <div class="backContainer">

                <form class="form-signin" asp-method="post" asp-controller="Account" asp-action="Login">
                    <h1 class="h3 mb-3 font-weight-normal">Please sign in</h1>

                    <div class="form-group">
                        <input type="text" class="form-control" id="username" name = "username" asp-for="username" placeholder="Username" autofocus>
                        <hr>
                        <small id="usernamehelp" class="form-text text-muted">We'll never require your Garena account.</small>
                    </div>
                    <div class="form-group">
                        <input type="password" name="password" class="form-control" asp-for="password" placeholder="Password">
                    </div>
                    <hr>
                    <div class="form-group form-check">
                        <input type="checkbox" class="form-check-input" id="exampleCheck1">
                        <label class="form-check-label" for="exampleCheck1">Remember me</label>
                        <a href="#">Forgot password?</a>
                    </div>
                    <button type="submit" class="btn btn-danger button">Log in</button>
                    <button type="button" class="btn btn-danger buttonfacebook">Log in with Facebook</button>
                </form>
            </div>
        </center>
    </div>*@

<div class="container">
    <center>
        <div class="backContainer">
            <form class="form-signin" asp-method="post" asp-controller="Account" asp-action="Login" id="Form">
                <h1 class="h3 mb-3 font-weight-normal">Please sign in</h1>

                <div class="form-group">
                    <input type="email" class="form-control" id="email" name="email" asp-for="Email" placeholder="email">
                    <small id="usernamehelp" class="form-text text-muted">We'll never require your Garena account.</small>
                </div>
                <div class="form-group">
                    <input type="password" class="form-control" placeholder="Password" name="password" asp-for="Password">
                </div>
                @if (ViewBag.isCorrect == false)
                {
                    <p style="color: red">Wrong email or password</p>
                }
                <div class="form-group">
                    <label for="RememberMe" style="color: white">Remember Me</label>
                    <input type="checkbox" value="true" class="form-control" id="RememberMe" name="RememberMe" asp-for="RememberMe">
                </div>
                <button type="submit" class="btn btn-danger button">Log in</button>

            </form>
        </div>

    </center>
</div>